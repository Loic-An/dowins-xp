{
  "name": "dowins-xp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "exit 0",
    "deploy": "wrangler deploy",
    "prestart": "node ./generateprivatekey.js",
    "start": "wrangler pages dev",
    "poststart": "kill -9 $(ss --listening --process --tcp | grep 'workerd' | awk -F 'pid=' '{print $2}' | awk -F ',' '{print $1}' | sort -u) >/dev/null 2>&1 || true",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "@cloudflare/workers-types": "^4.20240502.0",
    "vitest": "1.3.0",
    "wrangler": "^3.56.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jose": "^5.3.0"
  }
}